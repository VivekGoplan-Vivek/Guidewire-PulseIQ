{
  "name": "sample_db",
  "description": "A sample database",
  "tables": [
    {
      "name": "users",
      "columns": [
        { "name": "id", "type": "int", "comment": "User ID" },
        { "name": "name", "type": "varchar", "comment": "User Name" },
        { "name": "email", "type": "varchar", "comment": "User Email" }
      ],
      "constraints": {
        "primary": {
          "columns": ["id"]
        }
      }
    },
    {
      "name": "orders",
      "columns": [
        { "name": "id", "type": "int", "comment": "Order ID" },
        { "name": "user_id", "type": "int", "comment": "User ID (FK)" },
        { "name": "total", "type": "decimal", "comment": "Order Total" }
      ],
      "constraints": {
        "primary": {
          "columns": ["id"]
        },
        "foreign_keys": [
          {
            "columns": ["user_id"],
            "references": {
              "table": "users",
              "columns": ["id"]
            }
          }
        ]
      }
    }
  ]
}
